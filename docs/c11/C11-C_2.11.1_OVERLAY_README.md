# C11-C 2.11.1 overlay

Apply this overlay to the validated 2.11.0 state. It changes only active copy/music/coverage/tooling/docs; it is not a full project archive.

Main operational command after installation:

```powershell
.\tools\prototypes\c11c_bulk\run_c11c_visual_loops_subtype_music_coverage.ps1 -ResetReviewAssets
```

Expected physical coverage: 26 Visual Loop videos, one per authored grammar/subtype, all with audio.
