# C11-C 2.12.0 — Current State

## Checkpoint
2.12.0 is a presentation/toolchain iteration on the validated 2.11.1 baseline. It is overlay-only.

## Canonical Visual Loop naming

| technical_id | artistic_name | production_id |
|---|---|---|
| geometric | Geometric Waves | c11c_geometric_waves_v1 |
| fractal | Fractal Bloom | c11c_fractal_bloom_v1 |
| kaleidoscope | Sacred Symmetry | c11c_sacred_symmetry_v1 |
| particle_flow | Living Particles | c11c_living_particles_v1 |
| vector_field | Invisible Forces | c11c_invisible_forces_v1 |

`kaleidoscope`, `particle_flow`, and `vector_field` remain preserved historical technical aliases. They are not extra families.

## Grammar coverage
Invisible Forces now exposes seven explicit presentation grammars, including `scalar_potential`, required by the weekly production schedule. Total weekly slots: 27.

## Weekly production
`run_c11c_weekly_production_batch.ps1` publishes the fixed 27-slot schedule through the canonical protected production launcher to `artifacts/batch/week/XXXX/<day>/<slot>_.../`. Seeds are unique per slot by default.

## Monthly production
`run_c11c_monthly_production_batch.ps1` repeats the exact 27-slot schedule for each week, generates globally unique seeds across the month, and writes `artifacts/batch/month/YYYY-MM/week1..weekN/`.

## Audio
FAMILY_MUSIC_V4 revision 4.1.1 is semantic to family + seed, and Visual Loops additionally receive `visual_grammar`. The generator uses pentatonic note selection, long envelopes, loop-safe deterministic low-frequency noise, periodic low-pass modulation, and distinct profile timbres.

## Social copy
Generated public copy contains no emoji characters and is written UTF-8 without BOM. Fixed hashtags are always present: `#GenerativeArt #GodotEngine #LoopArt #OddlySatisfying`. Family/subtype hashtags are appended.

## Frozen boundaries
No C11-B simulation truth, structural RNG, SimulationResult, winning_frame, close_calls, WinningFrameDetector, RenderedFrameStream, C7 ownership, or C9 authoring contracts are modified by this iteration.

## 2.12.0 production scheduling
The weekly pattern is 27 slots (Mon-Sat 4 each, Sun 3) and includes Invisible Forces / Scalar Potential. Monthly production repeats the same pattern with globally unique seeds.

Canonical family nomenclature is stored in `profiles/presentation/c11c_visual_loop_family_nomenclature.json`.

## Production schedule
The canonical weekly schedule contains 27 slots: 4 Monday through Saturday and 3 Sunday. Invisible Forces has seven grammars so the Sunday-only Scalar Potential slot is represented explicitly.

## Continuity
Current next-context prompt: `docs/c11/START_PROMPT_C11C_NEXT_CONTEXT_2.12.0.md`.
