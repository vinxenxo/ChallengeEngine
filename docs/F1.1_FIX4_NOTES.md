# C6-F / F1.1 — FIX4

## Purpose
Correct the migration equivalence contract without changing any C3-C6 runtime contract.

## Changes
- `ChallengeMigrationAdapter.gd`: V2 -> runtime no longer removes `coordinate_space` or `secondary_binding` heuristically.
- `C6F1MigrationEquivalenceTest.gd`: runtime equivalence now compares only execution-relevant declarative inputs (`challenge_id`, `mechanic`, `generation`, `video`, `difficulty`, `content`).
- Presentation and asset data are excluded from C3-C5 simulation equivalence.
- Added separate preservation assertions for explicitly authored root metadata, content fields, presentation fields and asset-family metadata.
- Canonical V2 structural defaults may exist where required by the schema; this is validated separately from legacy runtime equivalence.

## Acceptance
1. All 9 legacy fixtures migrate and validate as canonical V2.
2. Runtime projection is byte-equivalent after normalization of representation types/defaulted absent `reveal_duration=0.0`.
3. Explicit presentation values survive migration.
4. `winning_frame`, frame count and frame position/rotation/opacity remain unchanged.
5. No changes to C3-C5/C6-E source contracts.
