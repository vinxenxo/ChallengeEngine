# MASTER HANDOVER — C11-C / ART DIRECTION 2.0 — v2.1.2

## Mission

Continue `ChallengeEngineV01_STATELESS` at C11-C. The current objective is Art Direction 2.0 over the five deterministic Visual Loop families.

## Frozen boundary

C11-B is CLOSED / CERTIFIED / FROZEN. Do not modify mechanic math, RNG architecture/ownership/version, native challenge durations, `SimulationResult`, `WinningFrameDetector`, `close_calls`, `RenderedFrameStream`, canonical visual content envelope, C7, C9, C11-B Header/Body/Footer geometry, or core/challenge simulation ownership.

C11-C is presentation-only.

## Five families

- `geometric` — Geometric Waves: harmonic loom / liquid architecture.
- `fractal` — Fractal Bloom: radial fractal nucleus, recursive branching, bioluminescent structure.
- `sacred_symmetry` — Sacred Symmetry: radial symmetry, concentric rings, astrolabe/mechanical geometry.
- `living_particles` — Living Particles: particles, attractors, eddies and fluid-like deterministic flow.
- `invisible_forces` — Invisible Forces: fields, traces, vortices, basins, lenses and topographic flow.

Common DNA: premium mathematical generative art, dark base, precise luminous traces, deterministic seed-driven variation, no external visual assets, no screensaver feel.

## Editorial system

Header contains two large lines. The first exposes real variation/math terms. The second is a human-facing hook and passes through a deterministic Matrix/airport-board character transition near the midpoint to the family identity before returning to the hook.

Decorative header/footer rules remain and use active palette colors.

Footer contains compact generation telemetry. It must NOT repeat `VISUAL LOOP // FAMILY`.

## Social delivery

Current baseline: **720×1280, 30 FPS, 18.00 s, 540 frames**.

The frozen base project remains 540×960. C11-C launchers do not edit `project.godot`; they temporarily write `override.cfg` overriding both viewport and window dimensions for Movie Maker capture, then restore/remove it. The prototype scene keeps the 540×960 logical composition and scales it 4/3.

Godot documents `override.cfg` as a project-settings override mechanism, and Movie Maker resolution follows effective viewport/window configuration. citeturn402691search0turn402691search12

## Audio

Default ON. `-NoSound` or `-Silent` disables audio. Audio is deterministic, low-profile ambient and must not change C7. One canonical MP4 exists per successful run; silent intermediates remain outside `artifacts` in the OS temp directory.

## Artifact separation

- `artifacts\prototypes`: experimental/review staging.
- `artifacts\production\audiovisual`: final products, protected.
- `legacy`, `qa`, `regression`, `releases`, `production`, `tests`: protected evidence roots.

Review/generation never cleans prototype artifacts. Cleanup/reset are separate explicit commands.

## Canonical commands

```powershell
.\tools\prototypes\c11c_bulk\validate_c11c_powershell.ps1
.\tools\prototypes\c11c_bulk\validate_c11c_delivery_configuration.ps1
.\tools\prototypes\c11c_bulk\run_c11c_art_direction_review.ps1 -ResetReviewAssets
.\tools\prototypes\c11c_bulk\run_c11c_production.ps1 -Family <family> -Seed <seed>
```

Do not use historical versioned `run_*_v2.0.x.ps1` / `run_*_v2.1.0.ps1` scripts for current work.

## Immediate task

Generate the 25-video Art Direction 2.0 corpus first. Then evaluate family identity, motion grammar, loop closure, mobile palette intensity, editorial hierarchy, decorative rules, typography, Matrix transition subtlety, diversity across seeds, and audio comfort.

Do not introduce seed-dependent duration yet. A later independent contract may map deterministic geometry/grammar/seed classes to short/medium/long loop durations.
