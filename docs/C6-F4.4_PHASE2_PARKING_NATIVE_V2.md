# C6-F4.4 Phase 2 — ParkingMechanicV2 Native Canonical V2

Promotes `parking_v2` to consume migrated Canonical V2 directly in the effective runtime.

## Frozen invariants

- No simulation mathematics changes.
- No RNG stream definitions or algorithms change.
- `SimulationResult`, `WinningFrameDetector`, and `VideoTimeline` semantics remain frozen.
- Legacy runtime remains isolated in `ChallengeLegacyRuntimeOracle`.
- Other mechanics retain the certified F4.3/F4.4 Phase 1 compatibility projection.

## Native V2 path

`simulation.parameters.parking` supplies spatial/mechanical configuration and `simulation.parameters.tolerance` supplies tolerance configuration. `simulation.seed` and `simulation.rng_version` are authoritative for V2 execution.

The effective parking runtime input contains no legacy `generation` section and does not restore root mechanical `difficulty.parking` or `difficulty.tolerance`.
