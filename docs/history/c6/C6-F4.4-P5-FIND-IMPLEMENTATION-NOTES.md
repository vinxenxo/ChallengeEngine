# C6-F4.4 Phase 5 — FIND Native Canonical V2

## Scope
Promote `find_v1` to native Canonical V2 execution.

## Intended changes
1. `FindMechanic.gd`
   - Use `simulation.parameters.find` as authoritative.
   - Fall back to legacy `difficulty.find`.
   - Apply this source rule consistently in `setup()`, `calculate_frame()`, and `simulate()`.
   - Do not change FIND equations, frame loop, RNG stream IDs, or result fields.
2. `ChallengeRuntimeBridge.gd`
   - Add `find_v1` to the existing native-V2 mechanic set.
3. `tests/C6F4CanonicalV2FindTest.gd`
   - Migration/schema gate.
   - Pure Canonical V2 execution with `generation`, `difficulty`, and `content` removed.
   - Effective runtime must contain no `generation` and no `difficulty.find`.
   - Independent legacy oracle.
   - Effective/oracle equivalence.
   - Direct/effective metrics and frame-count equivalence.
   - Explicit `distractor_topology` preservation checks.

## Frozen / forbidden
- RNG algorithms and stream definitions.
- FIND mathematical equations.
- `SimulationResult`.
- `WinningFrameDetector`.
- `VideoTimeline` / timeline mathematics.
- generic presentation composition.
- `build_factory.py`.

## Acceptance
The native V2 result must be deterministically equivalent to the legacy oracle for the frozen CHALLENGE_007 corpus, within the existing certified comparator tolerances.
