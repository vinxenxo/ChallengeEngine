# C6-F / F1.1 — FIX3

Fix focused on CHALLENGE_002 runtime projection equivalence.

The V2 canonical schema requires presentation structural fields, so migration supplies explicit canonical defaults. Those fields are authoring/schema boundaries, not evidence that the legacy V1 runtime originally contained them.

The V2 -> V1 runtime projection therefore removes only those exact migration defaults when reconstructing legacy runtime input. Explicit non-default presentation values remain intact.

No simulation, RNG, VideoTimeline, GeneradorMaestro, or C6-E files are changed.
