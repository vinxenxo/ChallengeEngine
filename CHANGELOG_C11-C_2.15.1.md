# C11-C 2.15.1 — Art Direction Contract Repair

- Repair `C11CArtDirectionProductionHygieneContractTest.gd` to use canonical family launcher paths (`geometric_waves`, `fractal_bloom`, etc.).
- Tighten Sacred Symmetry containment to `GEOMETRY_CONTAINMENT_SCALE = 0.82` and bound `macro_scale` to `0.78..0.96`.
- Declare and persist the `never_through_black` Longform transition contract.
- Update the Visual Drill social presentation contract for the shared 3-line header / 2-line footer nodes.
- Update `tests/run_all.py`: any initially failed suite is rerun once with Godot `--verbose`, with the diagnostic log saved under `artifacts/tests/logs/verbose/`; the original run remains authoritative for PASS/FAIL status.

No simulation, RNG ownership, C11-B frozen truth, C7 audio ownership, or C9 authoring contracts changed.
