# C11-C Studio v0.5.1 — UX visibility pass

- UI-only pass on Generate.
- GenerateSummary compressed to a single context row.
- Main generation workspace receives the reclaimed vertical space.
- Generate workspace wrapped in a vertical scroll area as a small-screen safety net.
- Interactive controls in Generate receive explicit minimum heights (46 px).
- Combo-box popup rows increased for readability.
- No changes to core/domain/services or canonical command logic.
