# C11-C 2.15.1 — Art Direction Contract Repair

This overlay repairs four regressions exposed by the first 2.15.0 E2E run.

## Repairs

1. Production hygiene contract now resolves the five canonical prototype launcher paths.
2. Sacred Symmetry containment now uses `0.82` containment and a conservative `macro_scale` clamp of `0.78..0.96`.
3. Longform composer explicitly declares `never_through_black` while retaining source-to-source `xfade=transition=fade` and a final fade-to-black.
4. Visual Drill social presentation contract now validates the dynamic shared header/footer labels and their 3-line / 2-line rendered blocks.
5. `tests/run_all.py` reruns every initially failed suite once with `--verbose` and writes the complete diagnostic output to `artifacts/tests/logs/verbose/`, without converting a diagnostic rerun into a PASS.

Scope remains presentation/toolchain/test-only; frozen simulation truth is unchanged.
