# C11-C 2.2.1 — Common Presentation Overlay

- Replaced the two-block header presentation with a single visible double-line header block.
- Preserved the former top header block's vertical space.
- Moved the header separator to immediately below the double-line block.
- Reduced the footer to separator + first text line.
- Moved the removed header/footer texts into a deterministic four-state Matrix sequence shown only in the double-line header.
- Enabled the same Matrix sequence for Visual Drills.
- Increased header Matrix typography to 21 logical px and footer typography to 14 logical px, with deterministic fit-down.
- Migrated all five Visual Loop prototypes to the shared `C11CVisualEditorialLayer`.
- Added the presentation-only `LivingParticlesTronRoad` perspective road with forward-travel animation and palette-derived contrast.
- No simulation, mechanic, RNG, SimulationResult, winning-frame, C7, C9 or frozen C11-B geometry changes.
