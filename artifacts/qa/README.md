# QA Evidence

C11 evidence is stored here after retiring the historical root `qa/`.

Some retained manifests/text logs contain the original pre-reorganization path names. Those strings are historical evidence and are not active filesystem dependencies. New executions must use the canonical `artifacts/qa/` paths.
